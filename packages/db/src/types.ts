// Type-only re-exports from Prisma generated models.
// Uses `export type *` (TS 5.0) which esbuild erases completely —
// zero runtime code reaches client bundles.
export type * from '@prisma/client';
